Breeze-up graduates — {{ run_date.strftime("%A %d %B %Y") }}
{{ total }} horse{{ '' if total == 1 else 's' }}

{% macro block(row) -%}
{{ row.horse_name or ('Lot ' ~ row.lot ~ ' (unnamed)') }}
  {% if row.sire or row.dam %}{{ row.sire or '?' }} x {{ row.dam or '?' }}{% if row.damsire %} ({{ row.damsire }}){% endif %}
{% endif %}
  Sale: {{ row.sale_name }} / Lot {{ row.lot }}{% if row.price %} / {{ row.price }}{% endif %}{% if row.buyer %} / {{ row.buyer }}{% endif %}
{%- endmacro %}

{% if ran_today %}
==== RAN TODAY ({{ ran_today|length }}) ====
{% for row in ran_today %}
{{ block(row) }}
  {{ row.course }}{% if row.off_time %} {{ row.off_time.strftime("%H:%M") }}{% endif %} - {{ row.race_name }}
  Finish: {{ row.finishing_position or '-' }}   SP: {{ row.sp or '-' }}
  {{ row.race_url }}

{% endfor %}
{% endif %}

{% if entered %}
==== ENTERED IN NEXT 5 DAYS ({{ entered|length }}) ====
{% for row in entered %}
{{ block(row) }}
  {{ row.race_date.strftime("%a %d %b") }} - {{ row.course }}
  {{ row.race_url }}

{% endfor %}
{% endif %}

{% if total == 0 %}
No breeze-up graduates ran today and none are entered in the next 5 days.
{% endif %}
